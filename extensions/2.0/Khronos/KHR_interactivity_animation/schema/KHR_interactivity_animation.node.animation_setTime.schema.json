{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "KHR_interactivity_animation.node.animation_setTime.schema.json",
    "title": "animation/setTime Node",
    "type": "object",
    "description": "Seeks animation to specific time (scrubbing). Events between old and new time do NOT fire.",
    "allOf": [{ "$ref": "glTFProperty.schema.json" }],
    "properties": {
        "type": {
            "const": "animation/setTime",
            "description": "Node type identifier."
        },
        "inputFlows": {
            "type": "array",
            "description": "Input flow sockets.",
            "items": {
                "type": "object",
                "properties": {
                    "id": { "const": "in" }
                }
            },
            "minItems": 1,
            "maxItems": 1
        },
        "outputFlows": {
            "type": "array",
            "description": "Output flow sockets.",
            "items": {
                "type": "object",
                "properties": {
                    "id": {
                        "anyOf": [
                            { "const": "out" },
                            { "const": "err" }
                        ]
                    }
                }
            },
            "minItems": 2,
            "maxItems": 2
        },
        "inputValues": {
            "type": "object",
            "description": "Input value sockets.",
            "properties": {
                "playbackId": {
                    "type": "object",
                    "description": "Handle from animation/start.",
                    "properties": {
                        "type": { "const": "int" }
                    },
                    "required": ["type"]
                },
                "time": {
                    "type": "object",
                    "description": "Time in seconds. Values outside [0, duration] are clamped.",
                    "properties": {
                        "type": { "const": "float" }
                    },
                    "required": ["type"]
                }
            },
            "required": ["playbackId", "time"]
        },
        "extensions": {},
        "extras": {}
    },
    "required": ["type", "inputFlows", "outputFlows", "inputValues"]
}
