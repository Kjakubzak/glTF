{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mesh.primitive.KHR_mesh_primitive_visibility_hint.schema.json",
  "title": "KHR_mesh_primitive_visibility_hint glTF Mesh Primitive Extension",
  "description": "Associates one mesh primitive with a non-inherited per-view visibility role. Evaluating the predicate does not alter glTF-authored or animated data; the implementation technique is not prescribed.",
  "type": "object",
  "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
  "properties": {
    "role": {
      "type": "string",
      "description": "Nonempty view-context role. The exact standard values are 'always', 'first_person', and 'third_person'; unrecognized values use the visible fallback under this extension.",
      "minLength": 1
    },
    "label": {
      "type": "string",
      "description": "Nonempty human-readable label for authoring tools and inspectors. It has no effect on visibility.",
      "minLength": 1
    },
    "extensions": { },
    "extras": { }
  },
  "required": ["role"]
}
