{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "KHR_character_expression_texture KHR_character_expression Extension",
    "type": "object",
    "description": "Classifies selected expression-animation channels as KHR_texture_transform property channels.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "channels": {
            "type": "array",
            "description": "Nonempty array of unique indices classifying KHR_texture_transform property channels. Classification does not select or filter channels for base expression evaluation.",
            "items": {
                "allOf": [
                    {
                        "$ref": "glTFid.schema.json"
                    }
                ],
                "description": "Index of a classified channel in the expression entry's selected animation.",
                "gltf_detailedDescription": "This value MUST be a valid index into the `channels` array of the animation selected by the containing expression entry's `animation` property. The channel MUST use KHR_animation_pointer to target offset, scale, or rotation on an actually authored KHR_texture_transform object."
            },
            "minItems": 1,
            "uniqueItems": true
        },
        "extensions": { },
        "extras": { }
    },
    "required": [
        "channels"
    ]
}
