{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "KHR_character_expression_mask KHR_character_expression Extension",
    "type": "object",
    "description": "Defines a nonempty set of masks originating from the containing expression entry.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "masks": {
            "type": "array",
            "description": "Nonempty array of masks whose implicit source is the containing expression entry.",
            "items": {
                "$ref": "KHR_character_expression_mask.mask.schema.json"
            },
            "minItems": 1
        },
        "extensions": { },
        "extras": { }
    },
    "required": ["masks"]
}
